#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

echo "Installing unzip..."
# Install unzip
sudo yum -y install unzip >> $DIR/../../sitc.log 2>&1

$DIR/../common/install_openmopac
